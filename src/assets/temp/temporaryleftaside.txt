<section className=" sticky left-0 top-0 z-20 flex h-screen w-fit flex-col justify-between overflow-auto border-r border-r-dark-4 bg-dark-2 pb-5 pt-28 max-md:hidden">
      <div className="px-3">
        {sidebarLinks.map((link) => {
          return (
            <div className="flex p-4 cursor-pointer rounded-full items-center gap-3 hover:bg-gray-800 ">
              <Link
                className="flex rounded-lg"
                key={link.label}
                to={link.route}
              >
                <img src={link.imgURL} />
              </Link>
              <p className="max-lg:hidden">{link.label}</p>
            </div>
          );
        })}
      </div>
    </section>